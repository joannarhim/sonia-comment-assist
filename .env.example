# AI mode: "mock" (default, no key needed) or "anthropic" (real LLM)
AI_MODE=mock

# Only needed when AI_MODE=anthropic
ANTHROPIC_API_KEY=your_key_here
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Where the SQLite decisions file lives
DB_PATH=decisions.db
